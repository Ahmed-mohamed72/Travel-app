import 'package:flutter/material.dart';

class SearhcView extends StatelessWidget {
  const SearhcView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Search View")),
    );
    ;
  }
}
